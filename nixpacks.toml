[phases.setup]
nixPkgs = ["python310", "postgresql"]

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[start]
cmd = "gunicorn victimes_pesticides.wsgi:application --bind 0.0.0.0:$PORT"
